{
  "name": "@vlcn.io/tsbuild-all",
  "version": "0.14.0",
  "main": "lib/index.js",
  "type": "module",
  "devDependencies": {
    "typescript": "~5.9.3"
  },
  "scripts": {
    "clean": "tsc --build --clean",
    "build": "tsc --build",
    "watch": "tsc --build -w",
    "deep-clean": "rm -rf ./dist || true && rm tsconfig.tsbuildinfo || true"
  }
}
